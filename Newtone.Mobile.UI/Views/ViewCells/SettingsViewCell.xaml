<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          xmlns:local="clr-namespace:Newtone.Mobile.UI"
          x:Class="Newtone.Mobile.UI.Views.ViewCells.SettingsViewCell">
    <ViewCell.View>
        <Grid HeightRequest="72">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Label Text="{Binding Name}" FontSize="16" FontFamily="{x:Static local:Fonts.Medium}" TextColor="{x:Static local:Colors.TextColor}" Margin="5,0" />
            <Label Text="{Binding Description}" Grid.Row="1" FontSize="16" FontFamily="{x:Static local:Fonts.Thin}" TextColor="{x:Static local:Colors.TextColor}" Margin="5,0" />

        </Grid>
    </ViewCell.View>
</ViewCell>