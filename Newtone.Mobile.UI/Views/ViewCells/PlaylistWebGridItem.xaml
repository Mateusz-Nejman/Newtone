<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:custom="clr-namespace:Newtone.Mobile.UI.Views.Custom"
             xmlns:local="clr-namespace:Newtone.Mobile.UI"
             x:Class="Newtone.Mobile.UI.Views.ViewCells.PlaylistWebGridItem">
    <ContentView.Content>
        <Grid HeightRequest="150" Margin="4,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="18"/>
            </Grid.RowDefinitions>

            <Image Source="{Binding Image}"
                   Grid.RowSpan="2"
                   Aspect="AspectFill"/>
            <Grid BackgroundColor="#99000000" Grid.Row="1"/>
            <Label 
                Text="{Binding PlaylistName}"
                TextColor="{Static local:Colors.TextColor}" 
                FontFamily="{x:Static local:Fonts.Regular}" 
                FontSize="14" 
                LineBreakMode="TailTruncation"
                HorizontalOptions="Center" 
                HorizontalTextAlignment="Center"
                Grid.Row="1"/>

            <custom:PressGestureMask 
                Grid.RowSpan="2" 
                LongPressedCommand="{Binding LongPressedCommand}" 
                PressedCommand="{Binding PressedCommand}"/>
        </Grid>
    </ContentView.Content>
</ContentView>