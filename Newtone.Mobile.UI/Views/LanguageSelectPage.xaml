<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:custom="clr-namespace:Newtone.Mobile.UI.Views.Custom"
             xmlns:local="clr-namespace:Newtone.Mobile.UI"
             x:Class="Newtone.Mobile.UI.Views.LanguageSelectPage"
             BackgroundColor="{x:Static local:Colors.ColorPrimary}">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <custom:CustomImageButton Grid.Column="1" Source="PolandFlag.png" Command="{Binding Polish}" Style="{StaticResource TransparentImageButton}"/>
            <custom:CustomImageButton Grid.Row="1" Grid.Column="1" Source="EnglandFlag.png" Command="{Binding English}" Style="{StaticResource TransparentImageButton}"/>
            <custom:CustomImageButton Grid.Row="2" Grid.Column="1" Source="RussiaFlag.png" Command="{Binding Russian}" Style="{StaticResource TransparentImageButton}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>