<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewcells="clr-namespace:Newtone.Mobile.UI.Views.ViewCells"
             xmlns:viewmodels="clr-namespace:Newtone.Mobile.UI.ViewModels"
             x:Class="Newtone.Mobile.UI.Views.DownloadPage">
    <ContentView.BindingContext>
        <viewmodels:DownloadViewModel/>
    </ContentView.BindingContext>
    <ContentView.Content>
        <ListView 
            SelectionMode="Single" 
            VerticalOptions="FillAndExpand" 
            ItemsSource="{Binding Items}"
            BackgroundColor="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <viewcells:DownloadViewCell/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentView.Content>
</ContentView>