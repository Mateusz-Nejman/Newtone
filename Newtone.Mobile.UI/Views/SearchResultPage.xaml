<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewcells="clr-namespace:Newtone.Mobile.UI.Views.ViewCells"
             x:Class="Newtone.Mobile.UI.Views.SearchResultPage">
    <ContentView.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <ListView Grid.RowSpan="2" SelectionMode="Single" VerticalOptions="FillAndExpand" RowHeight="56" ItemSelected="SearchListView_ItemSelected" ItemsSource="{Binding Items}" ItemAppearing="SearchListView_ItemAppearing">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <viewcells:SearchResultViewCell/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Grid IsVisible="{Binding SpinnerVisible}" Grid.Row="1">
                <ActivityIndicator IsRunning="True" VerticalOptions="Center" HorizontalOptions="Center" Color="White"/>
            </Grid>
        </Grid>
    </ContentView.Content>
</ContentView>