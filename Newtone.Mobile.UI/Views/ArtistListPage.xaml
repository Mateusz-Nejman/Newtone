<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewcells="clr-namespace:Newtone.Mobile.UI.Views.ViewCells"
             xmlns:viewmodels="clr-namespace:Newtone.Mobile.UI.ViewModels"
             x:Class="Newtone.Mobile.UI.Views.ArtistListPage">
    <ContentView.BindingContext>
        <viewmodels:ArtistListViewModel/>
    </ContentView.BindingContext>
    <ContentView.Content>
        <ListView 
            SelectionMode="Single" 
            VerticalOptions="FillAndExpand" 
            ItemSelected="ArtistListView_ItemSelected" 
            ItemsSource="{Binding Items}" 
            IsPullToRefreshEnabled="True" 
            RefreshCommand="{Binding Refresh}" 
            RefreshControlColor="#0f4c75" 
            CachingStrategy="RecycleElement"
            IsRefreshing="{Binding IsRefreshing}"
            BackgroundColor="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <viewcells:ArtistViewCell/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentView.Content>
</ContentView>