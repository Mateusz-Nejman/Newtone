<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewcells="clr-namespace:Newtone.Mobile.Views.ViewCells"
             xmlns:viewmodels="clr-namespace:Newtone.Mobile.ViewModels"
             mc:Ignorable="d"
             x:Class="Newtone.Mobile.Views.TracksPage">
    <ContentView.BindingContext>
        <viewmodels:TrackViewModel/>
    </ContentView.BindingContext>
  <ContentView.Content>
        <ListView SelectionMode="Single" VerticalOptions="FillAndExpand" ItemSelected="TrackListView_ItemSelected" ItemsSource="{Binding Items}" IsPullToRefreshEnabled="True" RefreshCommand="{Binding Refresh}" RefreshControlColor="#0f4c75" IsRefreshing="{Binding IsRefreshing}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <viewcells:TrackViewCell/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
  </ContentView.Content>
</ContentView>