<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:images="clr-namespace:Newtone.Mobile.Views.Images"
          xmlns:viewcells="clr-namespace:Newtone.Mobile.ViewModels.ViewCells"
          mc:Ignorable="d"
             x:Class="Newtone.Mobile.Views.ViewCells.TrackViewCell">
    <ViewCell.View>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="64"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Image Source="{Binding Image}" Aspect="AspectFill" Margin="5"/>
            <Label Text="{Binding TrackString}" Grid.Column="1" Style="{StaticResource ListViewText}" LineBreakMode="TailTruncation" VerticalTextAlignment="Center"/>
            <Image Source="PlayIcon.png" Grid.Column="2" Aspect="AspectFit" WidthRequest="16" HeightRequest="16" IsVisible="{Binding IsVisible}"/>
            <Label Text="{Binding Duration}" Grid.Column="3" Style="{StaticResource ListViewText}" VerticalTextAlignment="Center"/>
            <images:CustomImageButton Grid.Column="4" Style="{StaticResource TransparentImageButton}" Source="MenuIcon.png" x:Name="menuButton" Command="{Binding OpenMenu}" CommandParameter="{x:Reference menuButton}" Tag="{Binding Info}" IsVisible="{Binding AllowContextMenu}" Padding="10"/>
        </Grid>
  </ViewCell.View>
</ViewCell>