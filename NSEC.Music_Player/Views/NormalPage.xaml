<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:custom="clr-namespace:NSEC.Music_Player.Views.Custom"
             xmlns:images="clr-namespace:NSEC.Music_Player.Views.Images"
             xmlns:local="clr-namespace:NSEC.Music_Player"
             mc:Ignorable="d"
             x:Class="NSEC.Music_Player.Views.NormalPage">
    <ContentPage.Content>
        <Grid BackgroundColor="Red" RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            <Grid BackgroundColor="{Static local:Colors.ColorSecondary}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>
                <Label Text="" TextColor="{Static local:Colors.TextColor}" FontFamily="{Static local:Fonts.Medium}" Margin="20,0,0,0" FontSize="26" VerticalTextAlignment="Center" LineBreakMode="TailTruncation" x:Name="titleLabel"/>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <images:CustomImageButton Source="UploadPageIcon.png" Style="{StaticResource TransparentImageButton}" Grid.ColumnSpan="2" Grid.RowSpan="2" Padding="16" Clicked="SyncButton_Clicked"/>
                    <Grid Grid.Row="1" x:Name="badgeBubbleSync">
                        <Frame CornerRadius="32" Margin="4" BackgroundColor="{Static local:Colors.BadgeColor}"/>
                        <Label HorizontalTextAlignment="Center" VerticalTextAlignment="Center" Text="1" TextColor="{Static local:Colors.TextColor}" x:Name="badgeCountSync"/>
                    </Grid>
                </Grid>
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <images:CustomImageButton Source="DownloadPageIcon.png" Style="{StaticResource TransparentImageButton}" Grid.ColumnSpan="2" Grid.RowSpan="2" Padding="16" Clicked="DownloadButton_Clicked"/>
                    <Grid Grid.Row="1" x:Name="badgeBubble">
                        <Frame CornerRadius="32" Margin="4" BackgroundColor="{Static local:Colors.BadgeColor}"/>
                        <Label HorizontalTextAlignment="Center" VerticalTextAlignment="Center" Text="1" TextColor="{Static local:Colors.TextColor}" x:Name="badgeCount"/>
                    </Grid>
                </Grid>
                
            </Grid>
            <Grid BackgroundColor="{Static local:Colors.ColorPrimary}" Grid.Row="1" x:Name="container">
                
            </Grid>
            <Grid BackgroundColor="{Static local:Colors.ColorSecondary}" Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <images:CustomImageButton Source="PlayIcon.png" Style="{StaticResource TransparentImageButton}" Padding="16" Clicked="PlayerButton_Clicked"/>
                </Grid>
                <Grid Grid.Column="1">
                    <images:CustomImageButton Source="TrackIcon.png" Style="{StaticResource TransparentImageButton}" Padding="16" x:Name="tracksButton" Clicked="TracksButton_Clicked"/>
                </Grid>
                <Grid Grid.Column="2">
                    <images:CustomImageButton Source="ArtistIcon.png" Style="{StaticResource TransparentImageButton}" Padding="16" x:Name="artistsButton" Clicked="ArtistsButton_Clicked"/>
                </Grid>
                <Grid Grid.Column="3">
                    <images:CustomImageButton Source="PlaylistIcon.png" Style="{StaticResource TransparentImageButton}" Padding="16" x:Name="playlistsButton" Clicked="PlaylistsButton_Clicked"/>
                </Grid>
                <Grid Grid.Column="4">
                    <images:CustomImageButton Source="SearchIcon.png" Style="{StaticResource TransparentImageButton}" Padding="16" x:Name="searchButton" Clicked="SearchButton_Clicked"/>
                </Grid>
                <Grid Grid.Column="5">
                    <images:CustomImageButton Source="SettingsIcon.png" Style="{StaticResource TransparentImageButton}" Padding="16" x:Name="settingsButton" Clicked="SettingsButton_Clicked"/>
                </Grid>
            </Grid>
            <Grid BackgroundColor="{Static local:Colors.ColorSecondary}" Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.ColumnSpan="3" x:Name="backgroundGrid" IsVisible="False">
                    <Image Source="EmptyTrack.png" Aspect="AspectFill" VerticalOptions="Center" HorizontalOptions="Fill" x:Name="backgroundImage"/>
                    <Grid BackgroundColor="#99000000"/>
                </Grid>
                <images:CustomImageButton Padding="4" Source="EmptyTrack" x:Name="trackImage" Aspect="AspectFill" Clicked="TrackImage_Clicked"/>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Label Text="" FontSize="14" FontFamily="{x:Static local:Fonts.Black}" TextColor="{Static local:Colors.TextColor}" Margin="0,4,0,0" LineBreakMode="TailTruncation" x:Name="trackLabel"/>
                    <Label Text="" FontSize="14" FontFamily="{x:Static local:Fonts.Light}" TextColor="{Static local:Colors.TextColor}" Margin="0,0,0,4" LineBreakMode="TailTruncation" Grid.Row="1" x:Name="artistLabel"/>
                </Grid>
                <images:CustomImageButton Source="PlayIcon.png" Grid.Column="2" Style="{StaticResource TransparentImageButton}" Padding="16" x:Name="playButton" Clicked="PlayButton_Clicked"/>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>