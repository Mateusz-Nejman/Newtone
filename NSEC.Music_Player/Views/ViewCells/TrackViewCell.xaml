<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:images="clr-namespace:NSEC.Music_Player.Views.Images"
          mc:Ignorable="d"
             x:Class="NSEC.Music_Player.Views.ViewCells.TrackViewCell">
  <ViewCell.View>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Label Text="{Binding TrackString}" Style="{StaticResource ListViewText}" LineBreakMode="TailTruncation"/>
            <Image Source="PlayIcon" Grid.Column="1" Aspect="AspectFit" WidthRequest="16" HeightRequest="16" x:Name="playIcon" IsVisible="{Binding IsVisible}"/>
            <Label Text="{Binding Duration}" Grid.Column="2" Style="{StaticResource ListViewText}"/>
            <images:CustomImageButton Grid.Column="3" Style="{StaticResource TransparentImageButton}" Source="MenuIcon.png" Clicked="CustomImageButton_Clicked" Tag="{Binding Info}" IsVisible="{Binding AllowContextMenu}"/>
        </Grid>
  </ViewCell.View>
</ViewCell>