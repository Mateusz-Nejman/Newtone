<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:custom="clr-namespace:NSEC.Music_Player.Views.Custom"
             xmlns:local="clr-namespace:NSEC.Music_Player"
             xmlns:localization="clr-namespace:NSEC.Music_Player.Languages"
             mc:Ignorable="d"
             x:Class="NSEC.Music_Player.Views.SettingsPage"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="{x:Static local:Colors.ContainerBackground}">
    <ContentPage.Content>
        <Grid RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="0"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <custom:CustomTitleView IsBackButton="True" Title="{x:Static localization:Localization.Settings}"/>
            <ScrollView VerticalScrollBarVisibility="Never" HorizontalScrollBarVisibility="Never" Orientation="Vertical" Grid.Row="1">
                <StackLayout>
                    <ListView x:Name="settingsList" VerticalOptions="FillAndExpand" ItemSelected="SettingsList_ItemSelected" RowHeight="64">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <custom:SettingsListViewCell/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackLayout>
            </ScrollView>
            <custom:PlayerPanel Grid.Row="2" HeightRequest="40"/>
            <Label Grid.Row="3" x:Name="forSender"/>
        </Grid>
        
    </ContentPage.Content>
</ContentPage>