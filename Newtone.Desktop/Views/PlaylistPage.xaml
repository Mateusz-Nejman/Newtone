<UserControl x:Class="Newtone.Desktop.Views.PlaylistPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Newtone.Desktop.Views"
             xmlns:datatemplates="clr-namespace:Newtone.Desktop.Views.Custom.DataTemplates"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Playlisty" Style="{StaticResource PageTitle}"/>

        <ListView Grid.Row="1" Background="#141414" x:Name="listView" SelectionChanged="ListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding TrackElem}" Style="{StaticResource ListViewText}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ListView Grid.Row="1" Grid.Column="1" Background="#121212" x:Name="trackListView" PreviewMouseLeftButtonUp="TrackListView_PreviewMouseLeftButtonUp" PreviewMouseRightButtonUp="TrackListView_PreviewMouseRightButtonUp">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <datatemplates:TrackDataTemplate/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
