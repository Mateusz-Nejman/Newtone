<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          xmlns:mobile="clr-namespace:Nejman.Newtone.Mobile"
             x:Class="Nejman.Newtone.Mobile.Views.ViewCells.ArtistViewCell">
    <ViewCell.View>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="64"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Image Source="{Binding Image}" Aspect="AspectFill" Margin="5"/>
            <Label Text="{Binding Name}" Grid.Column="1" Style="{StaticResource ListViewText}" LineBreakMode="TailTruncation" VerticalTextAlignment="Center"/>
            <Label Text="{Binding Duration}" Grid.Column="2" Style="{StaticResource ListViewText}" VerticalTextAlignment="Center"/>
            <ImageButton Grid.Column="3" Source="MenuIcon.png" x:Name="menuButton" Command="{Binding MenuCommand}" CommandParameter="{x:Reference menuButton}" Padding="10" BackgroundColor="{x:Static mobile:Colors.PrimaryBackground}"/>
        </Grid>
    </ViewCell.View>
</ViewCell>