<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          xmlns:mobile="clr-namespace:Nejman.Newtone.Mobile"
             x:Class="Nejman.Newtone.Mobile.Views.ViewCells.DownloadViewCell">
    <ViewCell.View>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Label Text="{Binding Title}" TextColor="White" Margin="5" FontSize="15" FontFamily="{x:Static mobile:Fonts.Light}" VerticalTextAlignment="Center"/>
            <Label Text="{Binding ProgressString}" Grid.Column="1" TextColor="White" Margin="5" FontSize="15" FontFamily="{x:Static mobile:Fonts.Light}" VerticalTextAlignment="Center"/>
        </Grid>
    </ViewCell.View>
</ViewCell>